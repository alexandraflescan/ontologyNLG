class Pattern:
    def __init__(self, text, number_ranges=0, number_domains=0, number_propertie=0, number_generic=0):
        self.text = text
        self.number_ranges = number_ranges
        self.number_domains = number_domains
        self.number_properties = number_propertie
        self.number_generic = number_generic